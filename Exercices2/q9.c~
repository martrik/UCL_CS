#include "stdio.h"

int main() {
  int reversing = 0;

  int i;
  for (i = 0; i<7; i++) {
    if (i == 4) reversing = 1;
    int rows = i+1 -reversing*(i%4);
    int t;
    for (t = 0; t<rows; t++) {
      if (t == 0 || t == rows-1) printf("*");
      else if () 
    }

  }



  return 0;
}
